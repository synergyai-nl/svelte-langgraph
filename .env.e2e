AUTH_TRUST_HOST="true"
AUTH_OIDC_CLIENT_ID="svelte-langgraph"
AUTH_OIDC_CLIENT_SECRET="secret"
AUTH_OIDC_ISSUER="http://localhost:8080"
AUTH_SECRET="test-secret-for-e2e-testing-only-not-for-production-use"
PUBLIC_LANGGRAPH_API_URL="http://localhost:2026"
PUBLIC_SENTRY_DSN=""
NO_COLOR="true"
OPENAI_API_KEY="not-used"
OPENAI_BASE_URL="http://localhost:8100/openai"
CHAT_MODEL_NAME="gpt-4o-mini"
# E2E database, dropped and recreated on every run by
# apps/backend/scripts/reset_test_db.py. Must name a different database than
# DATABASE_URL (the reset script enforces this before dropping anything).
TEST_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/aegra_e2e"
AUTH_TYPE="custom"
