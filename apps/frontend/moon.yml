$schema: 'https://moonrepo.dev/schemas/project.json'

language: typescript
stack: frontend
type: application

fileGroups:
  sources:
    - 'src/**/*'
    # Configs
    - '**/*.config.*'
    # Other files
    - '**/*.{md,mdx,yml,yaml,json}'
  tests:
    - 'tests/**/*'
    - 'e2e/**/*'
  messages:
    - 'project.inlang'
    - 'messages'

tags: ['vite', 'vitest', 'sveltekit']

tasks:
  clean:
    # Also wipe .svelte-kit: a stale generated/server/internal.js bakes an old
    # __sveltekit_<version_hash> into the SSR bundle while the client pass computes
    # a fresh one from live config — splitting the build and breaking hydration.
    command: 'rm -rf build .svelte-kit src/lib/paraglide'
    options:
      cache: false

  build:
    deps:
      - clean
    inputs:
      - '@group(sources)'
      - '@group(messages)'
    outputs:
      - 'build'
      - 'src/lib/paraglide'

  # `check`, `test`, and `test-cov` share a mutex: the vitest tasks' paraglide plugin REGENERATES
  # src/lib/paraglide at startup, so any of them running while another reads those files (or
  # .svelte-kit/) sees half-written modules — the #290 flake family (svelte-check "is not a
  # module", vitest "Cannot find module .../paraglide/messages.js"). Their `build` dep closes the
  # same race against the uncached `clean`.
  check:
    deps:
      - build
    options:
      mutex: 'frontend-generated'

  lint:
    command: eslint .
    inputs:
      - '@group(messages)'
      - '@group(sources)'
      - '@group(tests)'
      # Root configs, any format
      # - '/eslint.config.js'

  lint-fix:
    extends: lint
    args:
      - '--cache'
      - '--fix'
    local: true

  format:
    command: prettier
    inputs:
      - '@group(messages)'
      - '@group(sources)'
      - '@group(tests)'
      - '.prettierignore'
      - '.prettierrc'
      # Root configs, any format
      # - '/.prettierignore'
      # - '/.prettierrc'
    args:
      - '.'
      - '--check'

  format-write:
    extends: format
    args:
      - '.'
      - '--cache'
      - '--write'
    local: true
    options:
      mergeArgs: 'replace'

  # `deps`/`mutex` rationale: see `check` above (#290).
  test:
    deps:
      - build
    options:
      mutex: 'frontend-generated'
    args:
      - '--silent'
      - 'passed-only'

  test-cov:
    deps:
      - build
    options:
      mutex: 'frontend-generated'
    args:
      - '--coverage.reporter text'
      - '--coverage.reporter lcov'
      - '--coverage.include src'
      - '--coverage.exclude "**/*.(test|spec).*"'
      - '--coverage.exclude src/lib/paraglide'
      - '--coverage.exclude src/lib/components/ui'
    inputs:
      - $GITHUB_ACTIONS

  test-ui:
    extends: test-cov
    args:
      - '--ui'
      - '--watch'
      - '--coverage.reporter html'
    preset: 'server'

  dev:
    options:
      envFile: '/.env'

  preview:
    options:
      envFile: '/.env'

  serve-e2e:
    extends: preview
    options:
      envFile: '/.env.e2e'
