$schema: 'https://moonrepo.dev/schemas/project.json'

language: typescript
stack: frontend
type: application

fileGroups:
  sources:
    - 'src/**/*'
    # Configs
    - '**/*.config.*'
    # Other files
    - '**/*.{md,mdx,yml,yaml,json}'
  tests:
    - 'tests/**/*'
    - 'e2e/**/*'
  messages:
    - 'project.inlang'
    - 'messages'

tags: ['vite', 'vitest', 'sveltekit']

tasks:
  clean:
    # Also wipe .svelte-kit: a stale generated/server/internal.js bakes an old
    # __sveltekit_<version_hash> into the SSR bundle while the client pass computes
    # a fresh one from live config — splitting the build and breaking hydration.
    command: 'rm -rf build .svelte-kit src/lib/paraglide'
    options:
      cache: false

  build:
    deps:
      - clean
    inputs:
      - '@group(sources)'
      - '@group(messages)'
    outputs:
      - 'build'
      - '.svelte-kit'
      - 'src/lib/paraglide'

  # These tasks all read generated SvelteKit and Paraglide files. Run them after
  # `build` has replaced everything removed by the uncached `clean`, and keep
  # them from reading while another task's plugin regenerates the same files.
  check:
    deps:
      - build
    options:
      mutex: 'frontend-generated'

  lint:
    command: eslint .
    inputs:
      - '@group(messages)'
      - '@group(sources)'
      - '@group(tests)'
      # Root configs, any format
      # - '/eslint.config.js'

  lint-fix:
    extends: lint
    args:
      - '--cache'
      - '--fix'
    local: true

  format:
    command: prettier
    inputs:
      - '@group(messages)'
      - '@group(sources)'
      - '@group(tests)'
      - '.prettierignore'
      - '.prettierrc'
      # Root configs, any format
      # - '/.prettierignore'
      # - '/.prettierrc'
    args:
      - '.'
      - '--check'

  format-write:
    extends: format
    args:
      - '.'
      - '--cache'
      - '--write'
    local: true
    options:
      mergeArgs: 'replace'

  test:
    deps:
      - build
    options:
      mutex: 'frontend-generated'
    args:
      - '--silent'
      - 'passed-only'

  test-cov:
    deps:
      - build
    options:
      mutex: 'frontend-generated'
    args:
      - '--coverage.reporter text'
      - '--coverage.reporter lcov'
      - '--coverage.include src'
      - '--coverage.exclude "**/*.(test|spec).*"'
      - '--coverage.exclude src/lib/paraglide'
      - '--coverage.exclude src/lib/components/ui'
    inputs:
      - $GITHUB_ACTIONS

  test-ui:
    extends: test-cov
    args:
      - '--ui'
      - '--watch'
      - '--coverage.reporter html'
    preset: 'server'

  dev:
    options:
      envFile: '/.env'

  preview:
    options:
      envFile: '/.env'

  serve-e2e:
    extends: preview
    options:
      envFile: '/.env.e2e'
