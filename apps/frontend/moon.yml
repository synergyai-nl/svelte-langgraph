$schema: 'https://moonrepo.dev/schemas/project.json'

language: typescript
stack: frontend
type: application

tags: ['vite', 'vitetest', 'sveltekit']

tasks:
  lint:
    command: eslint .

  lint-fix:
    extends: lint
    args: '--fix'
    local: true

  format:
    command: pnpm run format
